# Bootstrap4_Basic_Template
Bootstrap4_Basic_Template [Bootstrap Programming Cookbook]

1. Init github repository
2. clone it.
3. >npm install bootstrap
4. >npm init (add package.json) <git reset --hard HEAD~ //reset last commit and roll back changes)
5. add .gitignore
6. >git add, commit, push (check the github connection) (
6. +/* index.html